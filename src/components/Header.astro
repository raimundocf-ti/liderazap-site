---
import HeaderLink from './HeaderLink.astro';
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-md shadow-sm">
    <div class="max-w-6xl mx-auto px-6">
        <nav class="flex items-center justify-between h-20">
            <a href="/">
                <img src="/lidera-zap.png" alt="Logo Lidera ZAP" class="h-10 w-auto" />
            </a>

            <div class="hidden md:flex items-center gap-8">
                <HeaderLink href="/">Home</HeaderLink>
                <HeaderLink href="#duvidas">Dúvidas</HeaderLink>
                <HeaderLink href="#planos">Planos</HeaderLink>
                <HeaderLink href="#quem-somos">Quem somos</HeaderLink>
            </div>
        </nav>
    </div>
</header>
<style>
    nav a {
        color: var(--cinza-texto);
        font-weight: 500;
        transition: color 0.3s ease;
    }
    nav a:hover {
        color: var(--verde-principal);
    }
    nav a.active {
        color: var(--verde-principal);
        font-weight: 600;
    }
</style>